## Purpose of database folder
this folder contains all programs related to my attempts in creating a database and learning SQL.

### Study Notes
- SQL is the standard language used to manage data
- making SQLBMS program and a serverside scripting language like PHP.
- RDBMS Data is stored in an object called a table, which is a collection of related data enteries.
- Each table consists of fields. For example, in a table on customerIDs, fields would include customer name, postal code, address, etc.
- A record is each entry of that data.

#### SQL Statements
- Most actions performed on a database are done with SQL Statements.